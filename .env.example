# Resend API Configuration
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_123456789

# Email Configuration (Required)
# The email address where contact form submissions will be sent
CONTACT_EMAIL=your-email@example.com

# The "from" email address for sending emails
# Note: Must be a verified domain in Resend (use onboarding@resend.dev for testing)
FROM_EMAIL=onboarding@resend.dev

# Optional: Send auto-reply to form submitters
# Set to "true" to enable, leave empty or set to "false" to disable
SEND_AUTO_REPLY=false
